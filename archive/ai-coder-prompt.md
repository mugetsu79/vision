Traffic Monitor V2: AI Coder Prompt BlueprintInstructions for the User:Open your AI coding assistant (Cursor, Claude Code, etc.) in your empty project folder.Upload or provide the traffic_monitor_v2_spec.md document to the AI and say: "Read this architecture document. We will be building this application step-by-step. Acknowledge that you understand the architecture."Once the AI acknowledges, paste the following prompts one at a time, waiting for the AI to finish and verify each step before moving to the next.Prompt 1: Project Scaffold & Database FoundationGoal: Set up the folder structure, dependencies, and the SQLite schema for the hybrid VMS."Referencing the traffic_monitor_v2_spec.md document, execute the following:Create the exact folder structure specified in section 4.Create the backend/requirements.txt including FastAPI, Uvicorn, opencv-python, onnxruntime, ultralytics (for ByteTrack/utilities), requests, and any other necessary libraries.Write a Python initialization script in backend/data/ that creates a SQLite database (config.db). The sites table requires standard fields. The cameras table must include fields for RTSP URL, the 8 calibration coordinates (4 src, 4 dst), and a processing_mode string field ('edge' or 'central').Add a tracking_events table containing id, timestamp, camera_id, class_name (e.g., 'car', 'person'), and speed."Prompt 2: Computer Vision Core & MathGoal: Build the YOLO/ONNX pipeline and math modules."Focus on the AI and Math components in the backend.Implement backend/app/speed_calc.py to calculate the perspective transform matrix (cv2.getPerspectiveTransform) and map a bounding box's bottom-center coordinate to real-world speed over time using a moving average filter.Implement backend/app/tracker.py using onnxruntime to load yolo11n.onnx. Enforce the Execution Provider priority (TensorRT -> CUDA -> OpenVINO -> CPU).Inside tracker.py, implement CLAHE night-vision pre-processing on the Luma channel.The inference loop must accept a dynamic list of active_classes and drop any YOLO detections that do not match before passing the remaining boxes to ByteTrack."Prompt 3: Hardware Camera & Hybrid Engine WorkerGoal: Connect the cameras and configure the worker to support local queuing OR remote API posting."Build the camera handling and the inference worker engine.Implement backend/app/camera.py. Create a hardware-aware VideoCapture factory supporting nvarguscamerasrc, nvv4l2decoder, and standard cv2.VideoCapture with a FRAME_SKIP parameter.Implement backend/app/engine.py. This worker connects to the camera, runs tracker.py, and applies speed math.Crucial Hybrid Logic: The engine must accept a publish_mode. If central, it pushes frames and JSON to a local multiprocessing.Queue and inserts directly into tracking_events SQLite table. If edge, it uses the requests library to POST the JSON payload to the Master Node's /api/edge/telemetry endpoint."Prompt 4: FastAPI Master Node & Ingestion APIGoal: Expose endpoints, the LLM controller, and handle remote Edge node data."Now build Process A: the Master Node.Implement backend/app/llm_agent.py. Write a function that accepts a natural language string, mocks an LLM parser, and returns a list of matching COCO classes.Implement backend/app/main.py using FastAPI. Add CRUD endpoints for the DB (Sites and Cameras).Process Manager: On startup, query the cameras table. For any camera where processing_mode == 'central', spawn a local engine.py multiprocessing worker. Do not spawn workers for 'edge' cameras.Implement POST /api/edge/telemetry: This ingestion endpoint receives JSON from remote edge nodes, writes the event to the tracking_events DB, and pushes the data to the WebSocket.Add endpoints: GET /video_feed/{camera_id}, WS /ws/telemetry (broadcasts all metadata), and POST /api/query (LLM configuration).Add endpoints: GET /api/history (groups events by interval and class_name) and GET /api/export (CSV export)."Prompt 5: Frontend Foundation & Settings VMSGoal: Bootstrap the React SPA and the configuration interface."Move to the frontend/ folder.Initialize a React application using Vite and configure Tailwind CSS.Create the App layout with a navigation bar (Dashboard, History, Settings).Implement the pages/Settings.jsx component. This must interact with the FastAPI CRUD endpoints to manage Sites and Cameras. Ensure the camera form includes inputs for the RTSP URL, the 8 Homography mapping points, and a dropdown to select Processing Mode ('Edge' or 'Central')."Prompt 6: Dynamic Dashboard, Canvas Overlay, & HistoryGoal: Build the video UI and dynamic reporting views."Finally, build the main monitoring interfaces.Implement pages/Dashboard.jsx to render a grid layout of active cameras.Implement components/AgentInput.jsx for the chat bar, wiring it to /api/query.Implement components/VideoStream.jsx. Use an <img> tag for MJPEG, and Tailwind absolute positioning to perfectly overlay a <canvas>. Listen to /ws/telemetry and use ctx.strokeRect to draw bounding boxes natively.Implement components/DynamicStats.jsx. Dynamically map over the counts keys from the WebSocket payload to generate stat cards (e.g., separating buses, cars, and pedestrians).Implement pages/History.jsx. Include a date-range picker, granularity dropdown, and Chart.js historical line graph that dynamically creates datasets based on class_name keys. Include a 'Download CSV' button."