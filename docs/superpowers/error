We’ve isolated it well:

- auth works
- backend health works
- `/api/v1/models` is throwing a real backend 500

So the right next move is to capture the stack trace for that route.

Run this in one terminal:

```bash
cd "$HOME/vision"
docker compose -f infra/docker-compose.dev.yml logs -f backend
```

Then, in a second terminal, trigger the failing route again:

```bash
cd "$HOME/vision"
curl -i -sS \
  -H "Authorization: Bearer $TOKEN" \
  http://127.0.0.1:8000/api/v1/models
```

Then copy the error traceback that appears in the backend logs.

If you want a second useful check at the same time, run these too:

```bash
docker exec infra-postgres-1 psql -U argus -d argus -c '\d models'
docker exec infra-postgres-1 psql -U argus -d argus -c 'select id, name, version, task, format, input_shape, license from models;'
```

Why this is the right next step:
- the models route code is simple, so one traceback should identify the exact failure
- at this point, guessing would be worse than reading the real exception
- the `psql` output will tell us whether the table/schema or existing row data is the problem

Paste back:
1. the backend traceback from `logs -f backend`
2. the two `psql` outputs

With that, I should be able to tell you the exact fix.
