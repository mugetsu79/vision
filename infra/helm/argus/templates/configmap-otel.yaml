{{- if .Values.otelCollector.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "argus.fullname" . }}-otel
  labels:
    {{- include "argus.labels" . | nindent 4 }}
data:
  config.yml: |
{{ .Values.otelCollector.config | indent 4 }}
{{- end }}
